services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    volumes:
      - type: bind
        source: ./backend
        target: /app
    ports:
      - '3000:3000'
      - '5555:5555'
